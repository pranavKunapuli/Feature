angular.module("phiture").controller("LandingController", function($scope, $location, Firebase) {
    var ref = Firebase;

    $scope.clientLoginRedirect = function() {

    };

    $scope.performerLoginRedirect = function() {

    };

    $scope.signupLoginRedirect = function() {

    };
});
